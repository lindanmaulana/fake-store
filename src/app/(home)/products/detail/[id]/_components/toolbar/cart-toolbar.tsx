"use client"

import { Button } from "@heroui/react"

export const CartToolbar = () => {
    return (
         <Button variant="bordered" className="border bg-white text-black font-semibold">Lihat Semua Produk</Button>
    )
}